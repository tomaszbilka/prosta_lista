import UserList from "components/UserList";
import ScreenWrapper from "components/ScreenWrapper";

const Home = () => {
  return (
    <ScreenWrapper>
      <UserList />
    </ScreenWrapper>
  );
};

export default Home;
